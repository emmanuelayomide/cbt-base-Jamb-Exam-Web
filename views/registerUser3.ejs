<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="icon" rel="image/x-icon" href="/images/icon.png">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>STEch CBT Platform</title>

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet" />

  <!-- CSS -->

  <!-- boostrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-SgOJa3DmI69IUzQ2PVdRZhwQ+dy64/BUtbMJw1MZ8t5HZApcHrRKUc4W0kG879m7" crossorigin="anonymous">
   <!-- End of botsrap -->
  <link rel="stylesheet" href="/styles/style.css" />
</head>
<body id="bodyColor3">

  
        <form onsubmit="return crossCheckPassWord(event)" action="/thirdregistration3" method="post">
    
            <!-- The First FormLine List  -->
             <div class="topSpace">

           
<div id="centeralizedRegister">

    <%  if(locals.infoMessage) { %>
        <div class="messagePassed">
          <p class="infoMessage"> <%=infoMessage %></p>
       
      </div>  
    <%  } %>
<div class="ErroMessageFromPassWord">
    <p id="outPutMEssage"></p>
</div>

    <div class="dOrigin">
      
        <div class="d1">
            <h4>Account Settings</h4>
            <p>Manage how you log in and secure your account</p>
        </div>
        <div class="d2">
            <div class="twoDetails">
         <div class="nameSection">
            <label for="Username">Username</label>
            <input type="text" name="username" id="username" autofocus required>
              </div> 
               <div class="surnameSection">
                <label for="password">Password</label>
              <input type="password" name="password" id="mainPassword" autofocus required>
          </div>
        </div>
            <div class="twoDetails2">
                <label for="state">state of Origin</label>
        
 
        <% if (locals.stateNames && locals.stateNames.length > 0) { %>
          <select id="state" name="state" required>
              <option value="">Select a state</option>
              <% stateNames.forEach(function(state) { %>
            <option value="<%= state %>"><%= state %></option>
          <% }); %>
          </select>
          

        <% } else { %>
          <option value="" disabled>States could not be loaded</option>
        <% } %>
</select>
    
        </div>
        </div>
        <div class="d3">
            <div class="empty">
                <label for="address">Comfirm Password</label>
            <input type="password" name="password2" id="phoneNumber"  required>
    
        </div>
        </div>
    </div>
<hr>
<div class="nextBrn">
   <button type="submit">Next</button>
</div>
</form>
</div>

</div>

<script>
    function crossCheckPassWord(event){
             event.preventDefault();
        let password = document.getElementById("mainPassword").value;
        let comfirmPassword = document.getElementById("phoneNumber").value;
if(password.length <5 || password!=comfirmPassword){
    let message = "PassWord Must be Thesame and greater than 5 Character";
let outPutMEssage = document.getElementById("outPutMEssage");
outPutMEssage.innerHTML = message
outPutMEssage.style.color="red";

return false;
}
else{
let message = "Valid Password, riderecting in minutes";
let outPutMEssage = document.getElementById("outPutMEssage");
outPutMEssage.innerHTML = message
outPutMEssage.style.color="green";

 event.target.submit();
        return true;
}


    }

</script>

  </body>
  </html>